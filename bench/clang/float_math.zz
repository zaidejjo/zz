// bench: float arithmetic in a loop (exercises -ffast-math path).
import std.math

acc := 0.0
for i in range(50000) {
    x := float(i)
    acc = acc + math.sqrt(x + 1.0)
}
println(acc > 0.0)
println(math.floor(acc))
