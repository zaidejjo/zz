// Ambiguous conversions (two impls from the same source) are a compile error.
struct ErrA { msg: str }
struct ErrB { msg: str }

impl ErrA {
    func convert_to_ErrB(self) -> ErrB {
        ErrB{msg: self.msg}
    }
    func convert_to_ErrB2(self) -> ErrB {
        ErrB{msg: self.msg}
    }
}
