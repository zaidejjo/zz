// E2E: chan — thread-safe message passing
import std.io
import std.task
import std.chan

c := std.chan()
task.spawn(|_| {
    std.chan.send(c, 42)
})
val:int = std.chan.recv(c)
io.println(val)
io.println("channel_ok")
