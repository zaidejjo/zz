// E2E: `func main() -> Result<T, E>` success path — Ok exits 0.
// (ZZ spells `()` as `unit`; `Result<unit, E>` is the `Result<(), E>` shape.)
func main() -> Result<int, str> {
    println("in main")
    .ok(0)
}
