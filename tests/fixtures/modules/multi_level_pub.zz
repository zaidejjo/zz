// Test: pub re-export chain pattern.
// Full multi-file test is in loader/tests.rs (pub_reexport_chain).

pub base := 100
pub func scale(x: int) -> int {
    x * base
}

func main() {
    println(scale(5))
}
